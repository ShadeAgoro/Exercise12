tup = 'Hello'
print(len(tup))
# prints the length of the string variable - 5 characters in 'hello'

tup = 'Hello',
print(len(tup))
# output '1' results from one string being printed out

tup = 'Hello', 'Hi'
print(len(tup))
# e.g. if we add 'Hi', output would be 2 due to two strings in list